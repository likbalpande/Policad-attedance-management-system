{
  "name": "@platform/db",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "pnpm run typecheck",
    "build": "tsup",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@platform/enums": "workspace:*",
    "@platform/permissions": "workspace:*",
    "drizzle-orm": "^0.33.0",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@platform/tsconfig": "workspace:*",
    "dotenv": "^16.4.5",
    "drizzle-kit": "^0.24.2",
    "tsup": "^8.5.1",
    "typescript": "^5.5.4"
  }
}
